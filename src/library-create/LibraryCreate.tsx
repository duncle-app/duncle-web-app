import React from 'react'
import {LibraryManager} from "../control/library-manager";


interface LibraryCreateProps {
    libraryManager: LibraryManager;
}


export default (props: LibraryCreateProps) => {
    return (
        <>Mr Sir</>
    );
}